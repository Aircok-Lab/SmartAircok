import React from 'react';

// import Search from '../../../components/Search'

const Manage = () => {
  return (
		<section className='manage-container'>
      <section className='manage-tabs'>

      </section>

      <section className='manage-search'>
        
        {/* <Search cname={'manage-search'} lists={popuplists.slice(dvctab.current, popuplists.length)} listopt={1} setFunc={setsearchlists}/> */}

      </section>

      <section className='manage-lists'>
        
      </section>
    </section>
  );
};

export default Manage;